1、安装Python 3.9+
2、在工程根目录下，打开cmd，运行：
pip install -r requirements.txt
3、把config目录下的 api_config.json.template 重命名为 api_config.json
4、双击 run.bat 运行
5、点击“API配置”，配置本地或者远程的大模型API
6、每一关都模拟了一种命令注入的情况，只有在触发关键字的情况下，才能获得密码

详细使用方法：
https://wiki.bafangwy.com/doc/914/